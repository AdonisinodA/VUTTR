export interface IToll {
  id: number
  nome: string
  link: string
  descricao: string
  tags: string[]
}

export interface ITollDTO {
  nome: string
  link: string
  descricao: string
  tags: string[]
}
