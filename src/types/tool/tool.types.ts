export interface IToll {
  id: string
  nome: string
  link: string
  descricao: string
  tags: string[]
}

export interface ITollDTO {
  nome: string
  link: string
  descricao: string
  tags: string[]
}
